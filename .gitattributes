# Git pre-commit 훅 스크립트는 반드시 LF여야 셔뱅(#!/bin/sh)이 Git-for-Windows(MSYS)에서
# 정상 동작한다. core.autocrlf=true 인 이 저장소에서 .gitattributes 없이 체크아웃하면 CRLF로
# 변환되어 훅이 깨질 수 있다 — scripts/git-hooks/pre-commit 및 그 사본은 항상 LF로 고정한다.
scripts/git-hooks/* text eol=lf
